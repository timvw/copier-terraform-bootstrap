_min_copier_version: "9.0.0"
_answers_file: .copier-answers.yml
_subdirectory: template
_templates_suffix: ""

project_name:
  type: str
  default: "{{ _folder_name }}"
  help: "Project name used for the be.icteam:project tag"

project_source:
  type: str
  default: "{{ _project_source }}"
  help: "Location where the source code is stored (eg: git@github.com:timvw/copier-terraform-bootstrap.git)"
